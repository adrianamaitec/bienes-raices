export default function ArchitectApartments() {
    return (
        <div>
            <h1>Lista de departamentos (Arquitecto)</h1>
            {/* Aquí iría la lista de departamentos con opciones de CRUD */}
        </div>
    );
}